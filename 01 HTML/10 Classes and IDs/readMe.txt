Div - used to divide the body into sections.
Id and class are attributes of div
ID - used once (each div has different ID)
classes - can be used many times (divs can all have same class). Ex : styling diff div the same way.

div and span have same functionality.
div - block element
span - inline element