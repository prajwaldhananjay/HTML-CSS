  
!DOCTYPE is used to tell the browser what type of document it is

<html> is the parent tag
<head> and <body> are children tags
<head> is used to provide info about the page
<title> title shows up on the tab
<!-- to create a comment on VS code

On the browser (Ctrl U) for seeing the source code
(Ctrl /) to make a line of code as comment and vice versa